elasticsearch:
  host: localhost
  port: 9200
  username: a
  password: a

  index:
    name: board
    shards: 1
    replicas: 0

logging:
  level:
    root: info
    org:
      springframework:
        data:
          elasticsearch:
            core: trace